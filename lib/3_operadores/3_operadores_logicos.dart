void main() {
  // Operadores Lógicos
  // &&, ||, !
  // && é o operador de e
  // || é o operador de ou
  // ! é o operador de negação

  final sexo = 'M';
  final idade = 18;

  if (sexo == 'M' && idade >= 18) {
    print("Maior de idade");
  } else {
    print("Menor de idade");
  }
}
